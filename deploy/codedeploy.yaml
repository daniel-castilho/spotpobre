AWSTemplateFormatVersion: "2010-09-09"
Description: >-
  CodeDeploy application + blue/green deployment group for the Spotpobre API (runtime-deployment
  S9). This stack imports the ECS service and target groups from deploy/stack.yaml via CloudFormation
  exports, so apply it after the foundation stack and reuse is automatic across environments
  (the export names are prefixed with the Environment parameter).

  ECS AppSpecs are passed to CodeDeploy inline (RevisionType=AppSpecContent), not read from S3;
  the reference AppSpec is in deploy/appspec.yaml (submitted by the CLI/CI).

Parameters:
  Environment:
    Type: String
    Description: Deployment environment (staging/production) — prefix of the imported exports.
  TerminationWaitTimeMinutes:
    Type: Number
    Default: 30
    Description: Minutes to wait before terminating the blue task set on a successful deployment.
  DeploymentConfigName:
    Type: String
    Default: CodeDeployDefault.ECSCanary10Percent5Minutes
    Description: CodeDeploy deployment configuration. Canary 10% / 5 min gives an observation window.

Resources:
  # ------------------------------------------------------------------ CodeDeploy service role
  CodeDeployRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "spotpobre-${Environment}-codedeploy"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal: { Service: codedeploy.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSCodeDeployRoleForECS
      Path: "/"

  # ------------------------------------------------------------------ CodeDeploy application
  CodeDeployApplication:
    Type: AWS::CodeDeploy::Application
    Properties:
      ApplicationName: !Sub "spotpobre-${Environment}"
      ComputePlatform: ECS

  # ------------------------------------------------------------------ Blue/green deployment group
  CodeDeployDeploymentGroup:
    Type: AWS::CodeDeploy::DeploymentGroup
    DependsOn: [CodeDeployRole, CodeDeployApplication]
    Properties:
      ApplicationName: !Ref CodeDeployApplication
      DeploymentGroupName: !Sub "spotpobre-${Environment}-blue-green"
      ServiceRoleArn: !GetAtt CodeDeployRole.Arn
      DeploymentStyle:
        DeploymentType: BLUE_GREEN
        DeploymentOption: WITH_TRAFFIC_CONTROL
      DeploymentConfigName: !Ref DeploymentConfigName
      BlueGreenDeploymentConfiguration:
        TerminateBlueInstancesOnDeploymentSuccess:
          Action: TERMINATE
          TerminationWaitTimeInMinutes: !Ref TerminationWaitTimeMinutes
      EcsService:
        ClusterName:
          Fn::ImportValue: !Sub "spotpobre-${Environment}::ClusterName"
        ServiceName:
          Fn::ImportValue: !Sub "spotpobre-${Environment}::ServiceName"
      LoadBalancerInfo:
        TargetGroupPairInfoList:
          - TargetGroupPairInfo:
              TargetGroups:
                - Fn::ImportValue: !Sub "spotpobre-${Environment}::BlueTargetGroupArn"
                - Fn::ImportValue: !Sub "spotpobre-${Environment}::GreenTargetGroupArn"
              ProdTrafficRoute:
                ListenerArns:
                  - Fn::ImportValue: !Sub "spotpobre-${Environment}::ListenerArn"
      RollbackConfiguration:
        RollbackEnabled: true
        RollbackTriggers:
          - Type: CloudWatchAlarm
            Value:
              Fn::ImportValue: !Sub "spotpobre-${Environment}::Rollback5xxAlarmName"
          - Type: CloudWatchAlarm
            Value:
              Fn::ImportValue: !Sub "spotpobre-${Environment}::RollbackLatencyAlarmName"
          - Type: CloudWatchAlarm
            Value:
              Fn::ImportValue: !Sub "spotpobre-${Environment}::RollbackReadinessAlarmName"
          - Type: DeploymentFailure
            Value: CODE_DEPLOY_DEPLOYMENT_FAILURE

Outputs:
  CodeDeployApplicationName:
    Description: Name of the CodeDeploy application.
    Value: !Ref CodeDeployApplication
  CodeDeployDeploymentGroupName:
    Description: Name of the blue/green deployment group.
    Value: !Ref CodeDeployDeploymentGroup
  CodeDeployServiceRoleArn:
    Description: IAM role ARN assumed by CodeDeploy.
    Value: !GetAtt CodeDeployRole.Arn
